# Database
DATABASE_URL="file:./yahaml.db"

# Server Configuration
NODE_ENV="development"
PORT=3000
HOST=0.0.0.0

# Relay Server Configuration
RELAY_PORT=10001
RELAY_HOST=0.0.0.0

# UDP Listener Configuration
UDP_PORT=2237
UDP_HOST=0.0.0.0
UDP_TARGETS=

# Authentication
OAUTH_ENABLED=false

# Frontend (for production deployments)
# Leave empty for development (uses Vite proxy)
# Set to your API URL for production (e.g., https://api.example.com)
VITE_API_URL=
