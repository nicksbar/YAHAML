# AudioBridge plugin configuration for YAHAML (v1.4.1+)
# No pre-defined rooms; YAHAML creates them dynamically via admin API

general: {
  codec = "opus"
  sample_rate = 48000
  channels = 2
  bitrate = 128000
  default_prebuffering = 6
  record = false
}
